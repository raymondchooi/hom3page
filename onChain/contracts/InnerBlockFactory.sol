pragma solidity ^0.8.20;

/**
 * @notice Contract to handel minting and management for inner wall tokens
 * @notice To create an inner wall the owner must lock their block into this contracts
 *          to enforce a no-sale - helping protect owners of inner wall blocks.
 *
 * @dev Should we make it so that you have to have a minimal amount of blocks to lock to mint a sub wall?
 */

contract InnerBlockFactory {

}
